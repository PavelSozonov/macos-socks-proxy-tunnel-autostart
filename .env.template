# SSH connection settings
SSH_USER=username
SSH_SERVER=your-server.com
SSH_KEY_FILE=~/.ssh/id_ed25519

# SOCKS proxy port (default: 8090)
SOCKS_PORT=8090

# Optional: HTTP proxy port via pproxy (leave empty to skip pproxy setup)
# Useful for apps that don't support SOCKS (e.g., Docker Desktop free version)
HTTP_PORT=

# HTTP proxy port via gost (default: 8118)
# Pre-requisite: brew install gost
# Forwards http://127.0.0.1:GOST_HTTP_PORT → socks5://127.0.0.1:SOCKS_PORT
GOST_HTTP_PORT=8118
